workflows:
  ios:
    name: iOS Build
    max_build_duration: 60
    environment:
      vars:
        - key: FLUTTER_VERSION
          value: "stable"
    scripts:
      - name: Install Dependencies
        script: |
          flutter pub get
      - name: Build iOS
        script: |
          flutter build ios --release
